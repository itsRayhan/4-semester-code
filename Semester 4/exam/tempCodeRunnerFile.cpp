#include <bits/stdc++.h>
using namespace std;

double eh(int a)
{
    if (a == 0)
    {
        throw "error: pointed error";
    }
}
return sqrt(a);

int main()
{
    int a;
    cout << "enter x";
    cin >> x;
    throw
    {
        amni=eh(x);
        cout<<"correct :" <<amni;
    }
    catch (const char *msg);
    cout<<msg;
    return 0;
}